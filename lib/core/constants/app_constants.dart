class AppConstants {
  // Animation durations
  static const Duration shortAnimation = Duration(milliseconds: 200);
  static const Duration mediumAnimation = Duration(milliseconds: 300);
  static const Duration longAnimation = Duration(milliseconds: 500);
  
  // UI Dimensions
  static const double addressBarHeight = 48.0;
  static const double tabBarHeight = 40.0;
  static const double minTabWidth = 120.0;
  static const double maxTabWidth = 300.0;
  
  // Glassmorphism
  static const double glassBlur = 20.0;
  static const double glassOpacity = 0.3;
  
  // Neon effects
  static const double neonBlur = 10.0;
  static const double neonSpread = 2.0;
}

